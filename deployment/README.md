# Deployment

Directorio para guias y scripts de despliegue de IIS y Windows Server.

Los scripts versionados deben ser reutilizables y no contener secretos. Los valores por ambiente deben venir de variables, archivos locales ignorados por Git o un mecanismo aprobado de gestion de secretos.
